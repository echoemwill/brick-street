# Brick Street — core dependencies (licensed data path)
requests>=2.31.0
flask>=3.0.0
flask-cors>=4.0.0
PyJWT>=2.8.0

# Legacy / non-commercial scrapers only (scraper.py, fetch_prices.py,
# fetch_earnings.py). NOT needed for the licensed Finnhub path. Install
# only if you explicitly run the deprecated scrapers with ALLOW_SCRAPING=1.
# playwright>=1.44.0
# yfinance>=0.2.40
